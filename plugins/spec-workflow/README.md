# spec-workflow

Spec-driven autonomous build workflow for Claude Code: a GitHub Project board is the source of truth, tasks are implemented TDD-first by delegated dev agents, a single gate command decides advancement, and humans steer asynchronously through issue comments and UI-option pages.

Everything project-specific lives in the consumer repo's **`.claude/project.yaml`** (versioned, `schemaVersion: 2`, YAML — [schema](./schemas/project-config.schema.json)): boards + field ids, one or more specs with epics/task ranges/dependency guards, hard invariants, gate/dev commands, `docs[]` sets (where documentation lives + which code paths each covers — one set for a standalone repo, one per package for a monorepo; the reviewer blocks PRs that change covered behavior without updating them), the `delegation.identities` roster (each role's git author templates AND its ALLOWED `models`, resolved per-clone; a role may be an array of per-package identities routed by `covers` globs; `models` is additive — either the legacy flat Claude model-id array (unchanged), or an object `{claude: [...], codex: {capability: fast|balanced|deep-review|large-context}}` that also declares a Codex capability tier, read only by the Codex adapter — omitting `codex` never changes Claude behavior; CDX-020, normalizer in `identity_lib.py`'s `normalize_models()`), paths, and methodology knobs (`maxInProgress` — the concurrency knob: board WIP limit AND parallel implementation lanes, 1 = sequential; `iterativeUI`, `isolationSuite`, `autoMerge`/`mergeMethod` — autonomous PR review, approval, and merge in place of human approval; protocol in [`skills/build-next/references/auto-review.md`](./skills/build-next/references/auto-review.md) — and `feedback` (`true` shorthand for `{enabled: true, feed: .claude/feedbacks/feed.yaml, roles: [orchestrator], autoTriage: false}`) — structured per-iteration process feedback about the workflow itself, triaged into backlog/brain-note/graduate/upstream/ignore at retro time; mechanics in `scripts/feedback.py`. Also `work` (absent == `{ type: pr }`): `type: pr` (default, today's behavior) or `local` (no PR — review is `git diff <mainBranch>..<branch>`, approval recorded as an issue comment, the orchestrator squash-merges locally + pushes main); `sync.mode` (local only, absent == `realtime`) governs WHEN queued board mutations flush — `realtime`/`task-close`/`session-end`/`manual` — with `blocked`/`new-item` events always syncing immediately regardless of mode (the safety valve); resolved with defaults via `config.py get work.type`/`get work.sync.mode`, deferral decisions via `scripts/work-mode.sh should-sync <event>`; protocol in `skills/implement-task/SKILL.md`, `skills/build-next/SKILL.md`, and `references/auto-review.md` §5. The feed lives in `.claude/feedbacks/` (adjacent to `project.yaml`), a tracked, orchestrator-mediated archive — committed and pushed alongside code by default (opt out via the repo's own `.gitignore`), and never read or written by dev/reviewer subagents, same isolation as the identity brains). Refs in `evidence[]`/`routing.ref` are qualified with the emitting repo's own `project.name` (`<project.name>#N`) so a multi-project archive stays unambiguous — a foreign-qualified ref passes through verbatim, and the generalization ban on `summary`/`generalized` rejects qualified refs too, not just bare ones; `feedback.py migrate-qualify` is a one-shot, surgical, idempotent migration for a feed that predates this. Two more additive-only keys (#163): `methodology.entityKinds` (object, entity kind → home-role string, e.g. `{card: card-vault}`) lets `brain.py entity-index`/neural-view resolve an entity's canonical **anchor** note; `neuralView.entityEdgeColor` (a new top-level `neuralView` section; string, default/absent `"gradient"`) forces neural-view's cross-role entity-correlation edges to a flat CSS color instead of the default per-endpoint brain-color gradient. See [Identity brains](#identity-brains) below. Another additive-only top-level key (AST-002, #302, SPEC-ASSISTANT.md §6): `assistant` — the persistent, LLM-agnostic assistant this repo hosts, absent by default. Per §6.1 it is the SOLE authority for assistant identity/enabled state (the `.neural-network` marker carries no assistant flags); its `names` (first = main name, rest = aliases), `systemPrompt`, `llm: {provider, model}`, `capabilities` (per-capability `enabled`, e.g. `codex`/`claude-code`), and `observability` (per-backend `enabled`/`retainDays`/`maxMB` retention knobs, §10.3, 0 = unlimited) are validated by `scripts/assistant/config.py`'s `validate_assistant()`, wired into `validate-config.py`; §6.5's provider↔capability consistency (`openai` requires `codex` enabled, `claude` requires `claude-code` enabled) is enforced with an error naming both sides. Structure only — binary/auth resolution is AST-006 preflight, not this schema.

**Codex support (in progress)**: this plugin ships a `.codex-plugin/plugin.json` and is installable from this repo's `.agents/plugins/marketplace.json`; an end-to-end smoke test (`scripts/e0-smoke-manual.sh`) proves a real Codex session can discover and run an installed skill. Full per-host invocation docs, model configuration, and a compatibility matrix are tracked in [`docs/BACKLOG-CODEX-COMPAT.md`](../../docs/BACKLOG-CODEX-COMPAT.md) and land here once that work ships — `.claude/` remains canonical in the meantime.

**Editor support**: `project.yaml`'s first line is a `# yaml-language-server: $schema=…` modeline; the Red Hat YAML extension (`redhat.vscode-yaml`) reads it for hover + autocomplete. [`schemas/project-config.schema.json`](./schemas/project-config.schema.json) is the **authoritative reference** for every key: every property and enum value carries a `description`/`enumDescriptions` (checked mechanically by `tests/section-schema-lint.sh`'s `schema-lint.py` walk — a future undocumented key fails the gate), so hovering any key in an editor shows its explanation, possible values, and default. `setup-project` also writes a `.vscode/` `yaml.schemas` mapping and an extension recommendation. **Legacy**: a `.claude/project.json` (schemaVersion 1, old `delegation.devModel/reviewModel/prReviewModel` keys) is still read — normalized to the v2 shape with a deprecation warning; `init-config.sh` converts it to `project.yaml`. Requires **PyYAML** (`pip3 install pyyaml`).

## Skills

| Skill | Purpose |
|---|---|
| `craft-spec` | Assisted spec creation: plan-mode interview → numbered-§ draft → backlog → review gate |
| `setup-project` | Bootstrap a repo: board creation, `init-config.sh` auto-fill, validation, hygiene |
| `setup-assistant` | Scaffold (or edit the settings of) a bare-brain assistant repo — marker, `assistant:` config section, brain dirs, generated persona `AGENTS.md`, gitignores; idempotent, script-driven (`setup-assistant.sh`) |
| `seed-board` | Issues + board items from the backlog (idempotent) |
| `board` | All board reads/writes via `board.sh` (no hardcoded ids); comments are the human steering channel |
| `next-task` | `PICK` / `RESUME` / `BLOCKED` decision from priority, epic order, guards, and the WIP limit |
| `queue` | Read-only: renders the upcoming build-next picks (priority-first, with blocked reasons) — never mutates the board |
| `find-task` | Ranked search of existing board issues (open + closed) by title/body similarity — the read side of dedup |
| `create-inbound` | Search-first, dedup-gated capture of ad-hoc ideas/bugs/requests — the write side of dedup |
| `implement-task` | One task: brief a dev subagent (what/how/why), verify, drive the board |
| `ui-options` | Iterative UI mode: options page with favorite + aspect selection for the human |
| `refine-task-ui` | Run/resume a task's UI decision to completion, then capture the finalized design as real screenshots folded into the task's issue body with resolved acceptance criteria |
| `gate` | The single green-before-advance quality command |
| `auto-merge` | Status/on/off for autonomous PR review+merge (`methodology.autoMerge`); asks with flow previews when called bare |
| `pr-review-model` | Show/set the autonomous PR reviewer's allowed models (`delegation.identities.reviewer.models`); asks with options when called bare |
| `agent-identities` | Show/set per-role identities — name/email templates, allowed `models`, `covers` routing (per-clone resolution) |
| `concurrency` | Show/set `methodology.maxInProgress` — sequential (1, default) vs N parallel lanes; asks with trade-offs when called bare |
| `build-next` | One loop iteration — drive with `/loop /spec-workflow:build-next` |
| `brain` | Inspect/tend the per-identity zettel brains (recall/mint/prune/directory/entity-index); orchestrator-only |
| `knowledge-base-seed` | Seed/refresh a `knowledge` identity brain from project sources (specs, backlogs, design docs, READMEs, layout, git history, board epics from config) via `kb-seed.sh`; brain-only role, orchestrator-mediated |
| `checkpoint` / `handoff` | Pause/resume the loop via a local flag; session handoff docs |
| `dev-up` | Bring up the project's dev stack for QA |
| `neural-view` | Start/stop/status for the live JARVIS-style visualization of the identity brains |
| `feedback` | Emit structured process feedback about the workflow itself (`methodology.feedback`); triaged at retro time, or offers `retrospective` when run standalone |
| `retrospective` | On-demand retro — triage pending feedback and mint/prune/graduate brain notes, outside a `build-next` PR-close |
| `sync-project-configs` | Discover anchored repos (`.claude/.neural-network` marker) and bring their `.claude/project.yaml` up to this plugin's config surface via versioned sync rules; dry-run by default |
| `changelog-generate` | Read-only: renders a Markdown changelog section from git log grouped by conventional-commit type, since the last `spec-workflow--v*` tag; `--write <file>` prepends it to a named file |

## Scripts (`scripts/`)

`config.py` (the shared loader: finds `.claude/project.yaml`, normalizes legacy json, `get`/`json`/`path`/`set` verbs for bash callers — `set` surgically edits one key, leaving comments/formatting intact) · `board.sh` (next/show/move/prio/est/add/bug/adopt/flush/ensure-labels/list/issues/comment/edit-body/fields/config/metrics/audit — `add [--type bug|feature|inbound]` files work into Backlog with the matching label; `bug` is a thin alias of `add --type bug`; `adopt <issue#>` adds an EXISTING issue to the board, idempotently, and sets its initial Status (matching `add`'s behavior — item-add itself never sets Status, so without this an adopted item would land with no status; #92); `ensure-labels` idempotently creates any configured label (`labels.bug`/`labels.feature`/`labels.inbound`) missing on the repo — run by `setup-project` and `seed-board.sh` so `add`/`bug` never fail on a label that was only ever declared in config, not created on GitHub; board/issue listing (`next`/`list`/`move`/`prio`/`est`/`issues`, and `seed-board.sh`'s own item enumeration) paginates past gh's per-call `--limit` ceiling via `paginate.sh`'s escalating-limit loop — no fixed 400/500-item truncation; **item-id cache (#78)** — `move`/`prio`/`est`/`add`/`adopt` resolve an issue's project-item id from a local cache (`.claude/board-cache.json`, gitignored: issue# -> {itemId, lastKnownStatus}) before ever re-paginating the whole board: a cache HIT costs zero gh calls, a cache MISS costs exactly the one full item-list call that would have happened anyway, which refreshes the WHOLE cache as a side effect (so every other issue's lookup that session is then free too); `next`/`list`/`audit` are the only whole-board reads and refresh the cache as a side effect rather than reading it; every successful mutation updates its own cache entry, and a mutation rejected for a reason other than a rate limit (e.g. the cached id no longer resolves) drops the entry and re-resolves ONCE against a fresh full-board lookup before retrying; `add`/`adopt`'s post-create visibility poll is capped at 2 attempts with backoff (was 3, and 10 pre-#77), each attempt itself cache-aware, before deferring to the #77 queue; the cache file is excluded from `gate.sh`'s tree fingerprint (`tree-state.sh`), same treatment as `telemetry.jsonl`/`lessons.jsonl`, so a routine mutation never invalidates an unrelated still-current gate pass; all cache writes are best-effort (a read-only `.claude/`, e.g., never fails the underlying move/prio/est/add, matching `telemetry.py`'s own write philosophy); **rate-limit resilience** — `move`/`prio`/`est`/`add`'s item-add step/`adopt` (including their item-id LOOKUP, not just the mutation itself), on a GitHub rate limit, append the op to `.claude/board-queue.jsonl` (gitignored) instead of failing, print `QUEUED (rate-limited until <reset>): <op>` (reset from `gh api rate_limit`, works even when GraphQL itself is exhausted), and exit 0; **detection is probe-based, not just text-matched** — a cheap case-insensitive "rate limit" substring check runs first, but gh sometimes masks GraphQL exhaustion as an unrelated error with no "rate limit" text at all (e.g. `unknown owner type`), so any gh-project failure that misses the text match probes `gh api rate_limit` and treats `.resources.graphql.remaining == 0` as ground truth for rate-limited, `> 0` as a real error surfaced verbatim (never silently swallowed into a queue); `flush` replays the queue in order — a queued `move` is skipped if the target status already holds, and a re-trip mid-replay re-queues the remainder and exits 0; **flush concurrency (#92)** — flush is guarded by an `mkdir`-based lockdir mutex next to the queue file (bash-3.2-portable: macOS has no `flock` builtin, `mkdir` is atomic on any POSIX filesystem) — a second flush that can't acquire the lock SKIPS immediately with a one-line message instead of blocking or replaying concurrently, and a lock older than `BOARD_QUEUE_LOCK_TTL` (default 600s) is broken with a warning so a crashed flusher can't wedge the queue forever; **lock liveness probe (#104)** — every acquired lock also writes a pidfile (`$QUEUE_LOCK_DIR/pid`, the flush's own `$$`); a lock a later flush can't acquire is checked there FIRST via `kill -0` — a definitively-dead holder is broken IMMEDIATELY, without waiting out the TTL, so a crashed flush self-heals within seconds instead of sitting SKIPped for up to 600s; a live holder, or a pidfile-less lock (pre-#104), falls back to the TTL-age check unchanged; **replay durability (#104)** — an entry's bytes exist on disk (the moved-aside batch or `$QUEUE_FILE`) until its mutation is CONFIRMED applied, never a delete-then-apply window: the aside file is rewritten after every line is resolved (applied, requeued on rate limit, or written back on a real failure) to hold exactly the remaining unconfirmed work, so a crash at any point during replay loses nothing; a REAL (non-rate-limit) apply failure is written back to `$QUEUE_FILE` too, with a visible `flush: ERROR ... kept in queue for follow-up` message, instead of silently vanishing — the loop keeps processing the other lines in the same batch; replay never blindly truncates the queue file — it atomically `mv`s it aside (same filesystem, a single rename) before replaying, then appends any re-queued remainder back onto the LIVE queue path, which new `queue_append`s may have grown meanwhile, so a concurrent append can never land in a snapshot-then-clear gap and get silently lost; before replaying, the moved-aside batch is deduped by `(op, issue)` for `move`/`prio`/`est` — keeping only the most-recently-enqueued entry (by `ts`, never rewritten across a requeue) — so a stale requeued op tail-appended behind a newer op for the same issue can never re-apply and regress a value the newer op already advanced past; `next`/`list`/`show` auto-flush a non-empty queue first, and fail fast with `RATE-LIMITED until <reset> — work continues; mutations queue; retry reads after reset.` on their own rate limit (masked or not) instead of a masked error; `list`/`issues` gate on gh's exit code so a gh failure never reaches an ungated JSON parse — see `board-queue.sh`; **`audit`** (#76) reconciles board reality vs the repo and prints one `DISCREPANCY:` line per finding — open PRs whose body has no `#N` reference, `project.branchPattern`-matching branches with no In-progress item, In-progress items with no matching local branch, and (`work.type: local` only) merged `mainBranch` commits — the last 200 (the window is stated in the report unconditionally: `commit scan window: last 200 commits`, so a clean report never silently implies the whole history was checked) — whose subject has no `#N` reference; a documented allowlist of recognized orchestrator process-commit prefixes (`retro(`, `spec(`, `feedback(`, `config:`) is exempt from that last check but always counted and printed — never silently hidden — via `audit.py`; exits 1 on any discrepancy, 0 on a clean report) · `board-queue.sh` (the rate-limit queue/flush/detection logic `board.sh` sources; not meant to be run directly) · `paginate.sh` (shared `gh_project_items_json`/`gh_issues_json` pagination helpers sourced by `board.sh`/`seed-board.sh`) · `next.py` (picker: priority → epic rank → guards → WIP resume) · `audit.py` (the `board.sh audit` reconciliation logic described above; not meant to be run directly) · `guard-board-move.sh` (PreToolUse(Bash) hook, shipped in `hooks/hooks.json` so every consumer gets it automatically — blocks a `board.sh move <n> "In review"` unless `gate.sh` recorded a pass for the current tree state; parses the actual `board.sh` argv with shlex rather than substring-matching the command line, SW-011) · `guard-brain-access.sh` (PreToolUse(Read,Bash) hook, issue #237, CDX-031 gap #5 — shipped in `hooks/hooks.json` so every consumer gets it automatically; blocks direct, raw content reads of `.claude/identities/<role>/brain/**` for EVERY caller, orchestrator included, forcing all brain access through `brain.sh`'s own subcommands — `ROLE.md`/`DIRECTORY.md` stay unblocked since `brains.md` requires reading them verbatim; a `Read` on a brain path, or a Bash content-dump command [`cat`/`head`/`tail`/`less`/`more`/`sed`/`awk`/`grep`, or a `python3`/`node` one-liner containing `open(`/`readFile`] whose argv names a brain path, is blocked — the same shlex-tokenize + `bash -c`/`sh -c` recursion technique as `guard-board-move.sh`; a bare `find`/`ls` (discovery, no content dump) and repo-wide `Grep`/`Glob`/`grep -r` are deliberately NOT blocked, see docs/design/cdx-E3.md's #237 follow-up for the full scope rationale; fails OPEN (allows) on anything unparseable, the opposite trade-off from `guard-board-move.sh`'s fail-closed, because misclassifying an ordinary read as a brain dump would break unrelated work far more often than it would catch a real bypass; HOOK-ONLY, Claude Code specific — no equivalent interception point exists under Codex) · `guard-pr-create.sh` (PreToolUse(Bash) hook, issue #76 — NOT auto-registered in `hooks/hooks.json`, opt-in per consumer repo; blocks `gh pr create` unless the PR body references a board issue — `Closes #N`/`Fixes #N` or `<slug>#N` — with the same shlex-argv parsing discipline as `guard-board-move.sh`, and warns [never blocks] when the current branch doesn't match `project.branchPattern`; heredoc BODIES (text being written to a file, e.g. via `cat > f <<EOF`) are excluded from the scan entirely BEFORE tokenization — a quote-aware line scanner finds each unquoted `<<`/`<<-` operator and drops every line through its matching terminator (word-for-word; `<<-` strips leading tabs from both), so nothing inside a body — including a literal `&&`/`;` sitting in the WRITTEN text — can ever be mistaken for a real operator or invocation; ONLY THEN does `gh pr create` count as a real invocation, and only at a genuine command-start position [token 0, or right after `;`/`&&`/`||`/`|`/`&`] — an argument to some other command [e.g. `cat`] is never mistaken for one; `--body-file <path>` resolves against the hook's actual cwd, folding in a leading `cd`/`pushd` in the same command string, not unconditionally against the repo root; see "Enforcement hooks" below for the settings.json snippet that registers it) · `init-config.sh` (auto-fill board ids from a live project; writes `project.yaml`) · `seed-board.sh` · `preflight.sh` (load-time config/spec checks injected into spec-requiring skills; AST-006, SPEC-ASSISTANT.md §6.6 — when the current root carries the `.neural-network` marker, additionally runs `scripts/assistant/preflight.py`'s enumerated assistant checks [config parses/validates, provider↔capability consistency, each enabled capability's bin resolves via `shutil.which` AND is authenticated via the CLI's own status subcommand — `codex login status`/`claude auth status --json`, always under a timeout, never an LLM completion per §17.1] and prints one advisory line per verdict; silent when there's no marker; a positive verdict is cached [content+bin-path hash, 1h TTL] in `.claude/neural-view/assistant-preflight-cache.json` so repeat preflights skip the subprocess auth probes — a negative verdict is never cached) · `validate-config.py` · `merge-mode.sh` (auto-merge status/on/off + reviewer models/merge method; `preauth` checks whether this repo's `.claude/settings*.json` already allow-lists `gh pr merge`/`gh pr review`, advisory only; `preauth-snippet` prints the permissions block to add; `requirements [--refresh]` is the source of truth for whether GitHub itself demands a formal approving review — probes `project.mainBranch`'s branch protection (`required_pull_request_reviews`) and rulesets (a `pull_request` rule) via `gh api`, prints exactly one of `requirements: none`/`formal-review-required`/`unknown (<why>)`, and caches the verdict in `.claude/merge-requirements.json` (gitignored) for 7 days — see `references/auto-review.md` §3-§5, the no-interactive-branch decision table that replaces asking the human which of these applies) · `work-mode.sh` (`type`/`sync-mode` print the resolved `work.type`/`work.sync.mode`, defaulting to `pr`/`realtime`; `should-sync <transition|task-close|session-end|blocked|new-item>` prints `now` or `defer` per the mode's batching cadence, with `blocked`/`new-item` ALWAYS `now` — the safety valve; see SPEC §8.6) · `concurrency.sh` (status/set `methodology.maxInProgress` — sequential vs N parallel lanes) · `identity.sh` (per-role identity + allowed-models resolution — default roles `dev`/`reviewer`/`orchestrator`/`peer-reviewer` (the `/peer-review` findings-label consumer, no models default), any other role works too as long as it's declared under `delegation.identities.<role>` — `covers` path routing; `--check` runs in preflight; `on-behalf <author-role> [--committer <role>] [--co <role>]...` prints a commit recipe — a `flags:` line (committer `-c` options, before `commit`), a `commit-flags:` line (`--author=`, after `commit`), and Co-authored-by trailers — so a commit credits every participating role and pastes verbatim into the documented template) · `identity_lib.py` (shared resolution the two identity.sh modes import) · `brain.sh`→`brain.py` (per-identity zettel memory: mint/recall/directory/consult/prune/graduate/graduate-check/entity-index/index/explain/path; spreading-activation retrieval, stdlib only; AST-004 (SPEC-ASSISTANT §5a/§17.5): every whole-file write (notes, `links.json`, `DIRECTORY.md`, `entity-index.json`, `consults.json`, ...) is a temp-file-then-`os.replace()` atomic swap, and every write path — including recall's own link-fire bumps — is serialized by one cross-process `flock` per identities dir, reentrant within a process so a recall's internal `save_links()` call never self-deadlocks; AST-003: `mint()`/`recall()` are also importable library functions — explicit typed parameters, structured dict return, no argparse/stdout coupling, same side effects (links.json/`.activation.jsonl`/brain-events.jsonl) as the CLI, which is now a thin renderer over them — the surface the assistant engine (AST-010+) imports in-process; `explain <role> <slug>` (SPEC-GRAPHIFY §9 R9.1, GL-020) is a READ-ONLY interrogation card for one note — full body, the exact recall header (confidence + outcome tally + contested/stale, via the same `_format_header_line` helper recall uses), a `community: (pending GL-030)` placeholder seam, every inbound/outbound link with weight/fires/last (sorted by weight desc then the other note's slug), and the top 5 co-activated notes from a 2-hop spread seeded 1.0 on just this note (`_spread_activation`, the same traversal `recall` uses to strengthen links, called here with `mutate=False` so `links.json` is never touched); unknown role/slug exits non-zero with a GL-001-style message; `path <role> <slug-a> <slug-b>` (SPEC-GRAPHIFY §9 R9.2, GL-021) is a READ-ONLY shortest-link-path finder (BFS over `links.json`, stdlib) — links are UNDIRECTED for pathfinding (the stored `from->to` direction is a mint-order artifact, not a traversal constraint), neighbors expand in sorted-slug order at every node so equal-length paths resolve identically across runs, a disconnected pair prints `no path` at exit 0 (absence is an answer, not an error), `A`→`A` prints the single slug, and unknown role/slug exits non-zero with the same message style as `explain`; `index <role> [--rebuild]` (MEM-031) upserts an incremental SQLite embedding index (`<role>/brain/index.sqlite3`, gitignored) keyed by (slug, body-content-hash) — unchanged notes are skipped, changed notes are embedded via the external `embeddings` capability (`capability.sh embed embeddings`, overridable via `BRAIN_EMBED_CMD`) and left untouched (no error) when that capability is absent; `--rebuild` regenerates the table from scratch; consumed by `recall`'s hybrid seeding (MEM-032, see Retrieval above) via a `--k` flag (default 8); `graduate-check [role] [--threshold N]` is a READ-ONLY threshold-based graduation proposal listing — notes at/above `methodology.graduationThreshold` (default 3), non-graduated, each with a heuristic destination (ROLE.md rule / specs[].invariants entry / test-or-lint); graduation itself stays the human-visible `graduate` retro action, SPEC §8.3; **shrink guard** (#249, SPEC-GRAPHIFY §13) — `prune --apply` refuses non-zero, printing a would-remove summary and writing nothing, when it would remove more than `methodology.shrinkGuardFraction` (default 30%) of the brain's links AND more than a small absolute floor (5, a code constant, so pruning e.g. 2 of 3 links in a tiny brain never needs it); `--force` proceeds with a loud removal summary, `LinkPruned` events still emitted per link either way; `entity-index` (#163) regenerates `.claude/identities/entity-index.json` from every role's `entities:` frontmatter alone — a committed, derived, `DIRECTORY.md`-tier artifact correlating notes about the same real-world entity across roles, never read by recall/query, symlinked notes attributed to their physical home role only) · `kb-seed.sh`→`kb-seed.py` (GL-050 — explores specs/backlogs/`specs[].epics`/design docs/applied spec-deltas/root READMEs/AGENTS.md/CLAUDE.md/top-level layout/`git log` subjects and seeds/updates a `knowledge` identity brain, reusing `brain.py`'s note/link serialization and shrink guard by import; every note carries `source: seed`/`seed-path`/`seed-commit` provenance frontmatter (additive `KEY_ORDER` extension); idempotent — an unchanged source is never rewritten, a changed one updates its note in place, never a duplicate slug or a delete; a run that would update more than `methodology.shrinkGuardFraction` of existing notes refuses without `--force`) · `ui-hub.py` (Iterative UI decision hub) · `neural-view.py` (read-only brain-visualization server: `/graph` [nodes carry an `entities` list; cross-role `type:"entity"` edges (#163) are an anchor↔member star per entity, read from `entity-index.json` when present else derived live from frontmatter, plus a per-repo `entityEdgeColor` map], `/events`, `/note`, `/projects` [per-repo board state via board.sh], `/sessions` [best-effort local session discovery], `/assistant/*` [AST-010, SPEC-ASSISTANT.md §5a — mounted by delegating to `scripts/assistant/engine.py`'s `AssistantEngine`, which also owns the per-subsystem worker threads (distiller/tasks/traces/index) started/stopped with the server; `/assistant/status` reports `{engine, workers:[{name,alive}], assistants, outcome, candidates:[{name,aliases,root}], selected, gated}` (AST-021, SPEC-ASSISTANT.md §7.2-§7.4 — `outcome`/`candidates` are `discovery.scan`'s own `one`/`multiple`/`none` classification and candidate roster, the branch selector neural-view.html's boot sequence renders as a silent auto-select, a picker w/ Skip, or a red gate overlay respectively; `selected`/`gated`/`askAgain` are this engine's selection state — `gated` is true once `POST /assistant/skip` is called, or whenever `outcome` is `none` (AST-022, SPEC-ASSISTANT.md §7.5, issue #319 — this state is now PERSISTED, not engine-instance memory only: a small JSON file, `assistant-selection.json`, under the same `state_dir` this engine already receives, atomic tmp+rename write matching `default_store.write_default`'s pattern; loaded on every `AssistantEngine` construction, so a page reload, a second tab, or a restarted `neural-view.py` process over the SAME state dir sees the SAME selection — DISTINCT from `default_store`'s §6.3 machine-local DEFAULT NAME used by §7.6 terminal resolution, a different question the two mechanisms never merge, see `scripts/assistant/selection_store.py`'s module docstring; when `askAgain` is true a fresh boot forces `selected: null, gated: false` even though a prior selection is still on disk, so the picker shows again every load, but the `askAgain` FLAG itself keeps persisting until turned back off; terminal agreement is automatic and needs no separate code path — `neural-view.py assistant status`/`chat` are themselves thin HTTP clients against this same running server's `/assistant/*` routes); `POST /assistant/select {name}` resolves `name` case-insensitively against the current scan's candidate names/aliases (reusing `default_store`'s own name matcher), clears an earlier Skip, and persists, 404ing with the real candidate list on no/ambiguous match; **switch flow + activation digest (AST-024, SPEC-ASSISTANT.md §7.7/§7.8, issue #321)** — when a select CHANGES the resolved name away from a previously-selected one (an initial pick or a same-name reselect is not a switch and adds no new key to the response), the response gains a `digest` key: `{sinceTs, notesMinted: [{slug, strength, ts}, ...], exchanges, tasks: [], tasksSource: "pending-E4"}`, built by `scripts/assistant/digest.py`'s `digest(root, since_ts, role="assistant")` from the INCOMING assistant's own `<root>/.claude/brain-events.jsonl` (`NoteMinted` events) and `<root>/.claude/assistant/session.jsonl`, both filtered to strictly after that assistant's own `lastActive` timestamp (a new additive field on `selection_store`'s persisted JSON, `{name: isoTimestamp}`, stamped for the OUTGOING assistant at the moment of the switch); `traces.sqlite` (§10.2) doesn't exist yet (E4), so the `tasks` section is honestly `[]` with `tasksSource: "pending-E4"` rather than fabricated — see `docs/spec-deltas/ast-024.md` for the paste-ready §7.7/§7.8 clarifications this documents. §7.7's "keep both assistants' background work running throughout" holds because AST-010's worker registry (`self.workers`) is per-ENGINE, not per-assistant, and a switch never touches it; §7.7's "flush in-flight turn state" is a documented no-op today because the turn pipeline (§8.1) is synchronous per HTTP request with nothing queued across requests to flush. The page's `ast-switcher` row click renders this digest (`ast-digest` container, `ast-digest-note` rows, `ast-digest-empty` placeholder when nothing happened) alongside the existing re-select/refresh; `POST /assistant/skip` sets `selected: null, gated: true` (persisted) for the rest of this process's lifetime (until a later select) — `POST /assistant/chat` (below) refuses with a 403 while gated, per §17.9, WITHOUT affecting the pre-existing §7.6 terminal resolution flow (which never calls select/skip and stays ungated by default); `GET`/`POST /assistant/settings {askAgain}` (AST-022) reads/writes the persisted ask-again setting alone — a non-bool `askAgain` on POST is a clean 400, and the write takes effect on the NEXT boot, not the current session's already-resolved `selected`/`gated`; the page's voice ⚙ panel exposes this as an `ast-ask-again` toggle plus an `ast-switcher` candidate list (rows `ast-switcher-row`, selected one marked `ast-switcher-selected`) that re-POSTs `/assistant/select` and refreshes on click; `/assistant/history?n=N` (AST-014, SPEC-ASSISTANT.md §8.7) returns `{exchanges, warnings}` from the resolved assistant's fsync'd session transcript (`scripts/assistant/store.py`'s `SessionStore` — append-safe JSONL under `<root>/.claude/assistant/session.jsonl`, one write-flush-fsync per exchange so a crash loses at most the in-flight turn; a torn trailing line from a mid-write crash is skipped and reported in `warnings`, never raised; `n` defaults to 20, capped at 500]; `POST /assistant/chat` (AST-016, SPEC-ASSISTANT.md §7.6, issue #314) — the engine-core turn endpoint both the terminal (below) and a future overlay call: body `{message, assistant?}`, §7.6 resolution (flag → sole assistant → local default → error listing candidates, a clean 400 — never a turn attempt when nothing resolves) then `turns.run_turn` against the resolved assistant's persona/provider ON the request thread (§5a — no worker queue involved), then a durable `SessionStore.append_exchange` + `save_state`; returns `{text, chips, warnings}`. Rolling summary maintenance (AST-032, SPEC-ASSISTANT.md §8.2, issue #324) lives in `turns.py`, not `store.py`: `_advance_session_state` regenerates `session_state["summary"]` every `SUMMARY_REFRESH_EVERY_K_TURNS` (8) completed exchanges via a deterministic-extractive `summarizer` (default `default_summarizer`: concatenates the prior summary with `role: text` lines from the refresh window, then hard-truncates), capped in CHARS twice (the summarizer's own `cap_chars` argument, then a defense-in-depth `[:cap_chars]` slice on its return value so a custom summarizer can never blow the cap) — `store.py`'s existing atomic tmp-file + `os.replace` `save_state` persists whatever `session_state` it is handed, unchanged; `compose_context` assembles the system text as persona → roster → rolling summary → recalled notes → last N≤6 turns (summary BEFORE notes, not §8.2's literal listing order — the "note wins" ordering: a recalled note is this turn's sharper, independently-ranked signal while the summary can be up to K-1 turns stale, so placing the note later lets it override a contradicting stale summary by prompt-order recency alone, no contradiction-detection needed; see `docs/spec-deltas/ast-032.md`). `neural-view.py assistant status|default [NAME]|chat [--assistant NAME] <message...>` (AST-016) is the headless terminal surface over this same route table: `status`/`chat` are thin HTTP clients against the running server (clean `neural-view not running — start it with ...` message + nonzero exit if it isn't, no traceback); `default` reads/writes the machine-local default assistant directly (`scripts/assistant/default_store.py`, §6.3) and needs no server at all. The page's own consumer of `/assistant/chat`/`/assistant/history` is the T-key chat overlay (AST-023, SPEC-ASSISTANT.md §8.6, §5, §8.3, §8.5, issue #320) — T (outside any input/textarea/contenteditable) opens a bottom-center `ast-chat-overlay` dialog, Esc closes it; the log (`ast-chat-log`, rows `ast-chat-row[data-role=user|assistant|system]`) seeds from `GET /assistant/history?n=30` and a 1-3 `ast-chat-lastx` toggle re-renders however many of the cached exchanges are visible purely client-side (no re-fetch); Enter POSTs `/assistant/chat` — while that turn is in flight the input stays open and further Enters queue FIFO in `window.assistantChat.queue`, dispatching one at a time as each prior turn resolves (never concurrent, never dropped), with an elapsed-time state (`ast-chat-state`, ticking every second) that clears on completion; a reply's `chips` (already on the response — `_chips_from_recall`, wired since AST-013/#311, §8.3 — no engine change was needed for this task) render as `ast-chat-chips`/`ast-chat-chip`; three refusal branches — gated (`/assistant/status`'s `gated`/`outcome:"none"`, disables the input and states the reason), offline (a `/assistant/status`/`/assistant/history` fetch failure, disables the input and shows an `ast-chat-retry` affordance), and adapter/auth-expired (a non-2xx `/assistant/chat` response — surfaces the engine's §8.5 `error` text, e.g. the `codex login` instruction, verbatim as a `system`-role row, never resynthesized). Placeholder HUD-consistent styling only (matching `.hud`'s panel/border language structurally) — the real look is a pending human choice for the iterative-UI restyle round]) · `capability.sh` (MEM-030, SPEC-MEMORY.md §9.1/§9.1.1 — install & drive modular capabilities: self-contained venv + pinned model units that own their heavy dependencies so the stdlib-only core never grows a runtime dep; `install <name> [--dir DIR]` [idempotent, offline once downloaded], `healthcheck <name>` [exit 0 healthy / exit 3 UNAVAILABLE with one stderr notice — the single signal consumers branch on, absence degrades a feature and never breaks a flow], `embed` [stdin text lines → JSON float arrays via the venv entrypoint, first capability: embeddings, bge-small-en-v1.5 per OQ-4], `path`; install base `${CAPABILITY_HOME:-~/.claude/capabilities}/<name>`, `--dir` overrides for per-repo isolation/tests; manifest.json carries name/version/entrypoint/healthcheck/model/deps; the template contract for future capabilities) · `scripts/assistant/gates.py` (AST-017, SPEC-ASSISTANT.md §15, issue #315 — the E1 numeric gates harness: `--mode stub --out <path>` [CI, merge-gating, `section-assistant-gates.sh`] runs all five N1-N5 gates against the stub provider CLI, proving the p50/p95/variance/tool-use-rate/marker-barrier-kill/bounded-failure PLUMBING, never real numbers; `--mode real --root <dogfood-repo> --brain-role <role> --out docs/gates/ast-e1-results.json` [orchestrator/dogfood only, never CI, never invoked from a build-loop task] drives 20 REAL turns through `POST /assistant/chat` against whatever provider `--root`'s `assistant:` section configures (N1), runs `brain.recall` with the query-embed cache against `--root`'s real `--brain-role` brain (N2), and a real provider CLI pointed at a freshly isolated, logged-out `CODEX_HOME` (N5); N3/N4 always drive their "turn in flight" load through the stub CLI regardless of mode, since what they assert — HTTP/thread-contention isolation and engine/store crash-recovery — is provider-agnostic (see `run_n3`/`run_n4`'s own docstrings); each gate's result carries `passed` against its Sec15 threshold plus the raw samples that produced it; N1's `p95 <= 15s` verdict in the committed `docs/gates/ast-e1-results.json` is THE E2 unblock — the recorded evidence the board decision cites) · `scripts/assistant/discovery.py` (AST-020, SPEC-ASSISTANT.md §7.1, issue #317 — the single classification code path for assistant repos: `classify_repo(root)` walks marker presence [pure §6.1 anchor] then config authority to one of `candidate`/`no-marker`/`marker-unreadable`/`no-config`/`config-invalid`/`no-assistant-section`/`section-invalid`/`disabled`, each non-candidate kind carrying a human `detail` string; `scan(roots)` classifies every root fail-closed [a broken sibling root can never raise out of the scan] into a `ScanResult` whose `outcome` is `one`/`multiple`/`none`, the §7.2-§7.4 branch selector E2's selection UX consumes; `default_store.discover_candidate` (AST-007, §6.3) now delegates to `classify_repo` rather than re-walking the same checks) · `scripts/assistant/distill.py` (the distiller, AST-030, SPEC-ASSISTANT.md §5a/§9.2/§9.5, E3, issue #322 — `process_batch(identities_dir, root, exchanges, role="assistant") -> {minted, bumped}` is pure, testable logic with no threading of its own; `run_worker` is the loop body `engine.py`'s AST-010 `distiller` worker slot binds instead of its v1 heartbeat no-op. Batches every `DEFAULT_BATCH_N` (8) exchanges, never per-exchange (§9.2) — v1 synthesis is DETERMINISTIC-EXTRACTIVE only, never an LLM/provider-CLI call from a background thread (a keyword/entity heuristic over the batch transcript slice; modest quality is an accepted, documented trade-off — the pipeline shape is the deliverable). An exchange whose recall chips name an existing note BUMPS it (re-mint, strength+1) instead of minting a duplicate; v1 never merges/retires/aggregates (NG3). Every write goes through `brain.py`'s flock'd mint API — the distiller worker IS the engine's single writer thread for brain mutations, no extra locking here — and a mint triggers the embeddings index refresh (AST-031, §9.3) so new notes are recallable within one batch cycle. `_chat` enqueues one exchange-ref per turn via `_enqueue_distill`, `queue.Queue.put_nowait` — O(1), NEVER blocking the request thread (§9.5); on overflow (`DISTILLER_QUEUE_MAXSIZE`) the policy is DROP-OLDEST: the oldest queued item is evicted to make room for the newest, itself a non-blocking `_nowait` call, so a full queue never stalls a turn even briefly. A batch that raises logs to stderr and drops (park-and-continue) — the transcript is the durable source, a dropped batch is never retried in a tight loop and never kills the worker thread) · `scripts/assistant/observability.py` (event emitter + traces writer, AST-040, SPEC-ASSISTANT.md §10.1/§10.2/§10.6/§10.7, E4, issue #326 — `emit(q, root, event)` is enqueue-only, O(1), NEVER blocking a turn (drop + stderr on a full/closed queue, same posture as the distiller's own enqueue); `run_writer(q, stop_event)` is the loop body `engine.py`'s AST-010 `traces` worker slot binds instead of its v1 heartbeat no-op — the ONE writer thread for every assistant's traces, per-root sqlite connections held ONLY by that thread (single-writer discipline, no cross-thread handles). Each root gets its own `<root>/.claude/assistant/traces.sqlite` — the SAME directory `session.jsonl` already lives in and is ALREADY gitignored wholesale (`ignore\t.claude/assistant/`, AST-005/AST-014), so this file needed no new manifest line. WAL mode + `busy_timeout` on every connection; idempotent `CREATE TABLE/INDEX IF NOT EXISTS` schema (an `events` table: `seq` writer-assigned monotonic-per-root, reseeded from `MAX(seq)` at open, `ts`/`session_id`/`turn_id`/`span_id`/`parent_span_id`/`kind`/`skill`/`modality`/`status` all indexed first-class columns, plus a `payload` JSON column); batched `executemany` per drain cycle, bounded shutdown drain. `query(root, since=None, turn=None, limit=...)` is the read path (AST-043/AST-045 consume it) — read-only connections, `since` a `seq` cursor (never a timestamp), both filters index-backed (never `json_extract`). `engine.py`'s `_chat` emits one `turn_id`-linked sequence per turn: `turn.start` → (`recall.summary` + `provider.call`) on success, or a first-class `provider.error` (§10.6, linked by `turn_id`) on an `AdapterError` → `turn.end`; the distiller's `run_worker` takes an optional `traces_queue` and emits `distill.batch` once per processed batch. Retention (AST-041, pending) is the only thing allowed to delete rows — traces are prunable history, never a must-survive file like `session.jsonl` (§10.7)) · `feedback.py` (emit/pending/route/status/migrate-qualify/archive/archived on the `methodology.feedback.feed` yaml feed; enforces the generalization contract at emit time — extended to qualified `<slug>#N` refs too — and qualifies bare `#N` in `evidence[]`/`routing.ref` to `<project.name>#N` at emit/route time, with `migrate-qualify` as a surgical one-shot for feeds predating the contract; `archive` moves every document whose items are ALL routed into `.claude/feedbacks/archive/<YYYY-MM>.yaml` (month from the document's own `ts`), preserving the moved bytes exactly — never round-tripped through a YAML dump — leaving partially/un-routed documents in the active feed; atomic per touched file (temp file + rename) and idempotent; `archived [--since YYYY-MM]` reads those archive files (never the active feed) and renders every item with the exact same tab-separated format as `pending`, optionally filtered to documents at/after the given month) · `telemetry.py` (record/metrics on `.claude/telemetry.jsonl`; per-iteration transition/gate/review-round/task-close events, and the cycle-time/gate/rework/estimate report — see Telemetry below) · `sync-configs.py` (cross-repo config sync: discovers `.neural-network`-marked repos — or targets one via `--repo` — and applies an ordered, versioned list of sync rules to `.claude/project.yaml`, each a surgical text edit never a parse-and-redump; validates before AND after via `validate-config.py`, rolling back an edit that would leave the config invalid; dry-run by default, `--apply` to write/commit/push; a repo on a non-main branch or with any dirty tree is never touched directly — changes land via a temporary detached worktree off `origin/<mainBranch>`, pushed with `git push origin HEAD:<mainBranch>`; commit identity resolved per-target-repo via `identity.sh orchestrator` run with cwd inside that repo; rules: `strip-schema-data-key`, `ensure-feedback-key`, `sw062-feedbacks-migration`, `ensure-peer-reviewer-identity` — adds `delegation.identities.peer-reviewer` only when the target repo's own `.claude/settings.json` `enabledPlugins` shows `peer-review@<marketplace>` enabled, never forced; `ensure-serial-delivery` (#276) — adds `methodology.serialDelivery: true` ONLY when the key is absent, mirroring `ensure-feedback-key`'s surgical-insertion shape with a fixed value (no configurable flag); an existing explicit `true` or `false` is left untouched; `mem013-gitignore-managed-block` — reconciles the anchored repo's `.gitignore` managed block to this plugin's CURRENT `scripts/local-state.manifest`, by shelling out to `gitignore-sync.sh` (dry-run to detect/diff, real run to apply), so a repo that adopted the workflow before the manifest gained a new `ignore`-policy path gets it synced without a human re-running `setup-project`) · `snippet-lint.py` (gate-wide version-floor lint over every inline python3 snippet in `scripts/*.sh` and `tests/*.sh` — `python3 -c '...'`/`"..."` (either bash quote form) and every `python3 [-m MOD] [-] <<[-]DELIM`/`<<[-]'DELIM'`/`<<[-]"DELIM"` heredoc — PYTHON floor 3.9 via `ast.parse(..., feature_version=(3, 9))`, plus a supplementary f-string regex for the one PEP 701 nested-quote-in-`{}` pattern `feature_version` does not catch (verified empirically, not assumed — see the script's module docstring); a double-quoted `-c` body or an unquoted-delimiter heredoc can embed real bash interpolation (`$var`/`${var}`/`$(...)`/`` `...` ``), neutralized to a placeholder before parsing (best-effort — an interpolation/quote span that never closes is reported as an explicit `UNSCANNED` finding, never silently skipped); BASH floor 3.2 via mechanical construct checks — `mapfile`/`readarray`, `declare -A`, `${var,,}`/`${var^^}`, `${var:off:-len}` negative-length substrings, `&>>` — with a same-line `# bash4-ok: <reason>` marker (non-empty reason required) to suppress an unavoidable exception; #45, generalizing the single-pattern check that used to live in `tests/section-syntax.sh`) · `diff-symbols.py` (#86 — maps a unified diff's hunks to their enclosing symbol in the NEW file version, so "this diff only touches functions X and Y" is a one-liner instead of manual line-range grepping: `python`/`.py` via `ast` [nested as `Class.method`], `bash`/`.sh`/`.bash` via brace-matched `name() {`/`function name {`, and a plain-text fallback [nearest preceding heading for `.md`, `(file-level)` otherwise]; reads a diff on stdin, or runs `git diff` itself via `--range A..B`; `--json` for structured output; a deleted file's hunks map to `(deleted)`).

## Enforcement hooks

`guard-board-move.sh` and `guard-brain-access.sh` ship in this plugin's `hooks/hooks.json`, so every consumer repo gets the gate-before-review guard and the brain-isolation guard automatically on install (`guard-brain-access.sh` runs on both the `Bash` matcher, alongside `guard-board-move.sh`, and a dedicated `Read` matcher). `guard-pr-create.sh` (#76) does NOT — it's a more opinionated, blocking gate on `gh pr create` itself, so it's opt-in: add it as an additional `PreToolUse` hook in the consumer repo's own `.claude/settings.json` (or `.claude/settings.local.json`):

```json
{
    "hooks": {
        "PreToolUse": [
            {
                "matcher": "Bash",
                "hooks": [
                    {
                        "type": "command",
                        "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/guard-pr-create.sh\""
                    }
                ]
            }
        ]
    }
}
```

Run `board.sh audit` periodically (or at session/loop checkpoints) to catch drift the guards alone can't — board items and PRs/branches/commits that have already diverged, not just the next mutation.

## Identity brains

Each agent role (dev / reviewer / orchestrator, extensible) owns a **private** brain of atomic zettel notes under `.claude/identities/<role>/brain/` in the consumer repo. Brains give each identity durable memory that evolves **separately** — a hard product requirement. `ROLE.md` (per role) is the stable, human-owned instruction; starter templates ship in [`templates/identities/`](./templates/identities/).

- **Isolation** — only the orchestrator process reads or writes a brain. Subagents never see a brain path; recalled lessons reach them as pasted text in their brief. One role reads another's note only through a deliberate `consult`.
- **Retrieval** — `brain.sh recall <role> --paths ... --keywords ...` runs **spreading activation**: notes seeded by path-glob/tag match, activation flowing along `[[wikilink]]` edges (2 hops), emitted strongest-first within a token budget. Graduated notes stop being injected but still bridge links. **Hybrid seeding (MEM-032)** — when the role's `index.sqlite3` sidecar exists (built by `index`, see below), the seed set is the union of the keyword/glob matches above with the top-`--k` (default 8) embedding neighbors of the `--keywords` text, ranked/budgeted identically to a plain seed; absent the sidecar, recall runs exactly as before (no behavior change, same code path).
- **Evolution** — at each PR close the orchestrator runs a retro: `mint` notes in its own wording (strength bumps on re-mint), `prune` stale links, `graduate` proven lessons, regenerate `DIRECTORY.md`, run `archive` as the final feed step, then commit the routed feed, archives, and brain changes together as the orchestrator identity.
- **Contract** — every recall/consult appends to `<role>/brain/.activation.jsonl` (a frozen JSON-lines format a live viewer consumes). Link metadata (weight/fires/last) lives in `links.json`; notes stay clean markdown.
- **Cross-identity correlation (#163)** — a note MAY declare an optional `entities: [kind:slug, ...]` frontmatter list naming the real-world thing(s) it's about (e.g. `card:gone-in-a-flash`), independent of the note's own slug scheme; `mint --entities "card:x,card:y"` sets it (like `tags`/`paths`, re-pass it on re-mint to keep it). `brain.sh entity-index` derives `.claude/identities/entity-index.json` — a committed, `DIRECTORY.md`-tier artifact mapping each entity key to every role's notes about it, plus an `anchor` (the entity's canonical note, resolved via `methodology.entityKinds`, a kind→home-role map, when that role has exactly one declaring note). This is metadata about **aboutness**, never content: per-role `recall`/`query` never read it (byte-identical output whether or not a note carries `entities`), and reading a correlated note still requires the same logged `consult` as always — the index only makes existing cross-role knowledge *findable*, never directly readable. `ask-brain` consults correlated notes across roles automatically (logged, attributed per role); `ask-identity` is awareness-only — it may say another identity holds N notes about an entity and point at it, but never pulls that content into its own answer (design doc: [`docs/design/cross-identity-correlation.md`](../../docs/design/cross-identity-correlation.md)).

Full protocol: [`skills/build-next/references/brains.md`](./skills/build-next/references/brains.md).

### Brain-event feed (`.claude/brain-events.jsonl`)

A single per-repo append-only JSON-lines file — the **episodic** record of memory operations across every role, distinct from each role's private `<role>/brain/.activation.jsonl` (which stays a frozen, byte-identical contract). `brain.py`'s `emit_event(root, obj)` appends one `\n`-terminated line per semantic event in a **single `write()`** to an `O_APPEND` file descriptor; on POSIX, whole-line appends under `PIPE_BUF` (~4KB) are atomic, so concurrent emitters from parallel processes never interleave or lose writes. The feed is **never load-bearing**: if the append fails (e.g. unwritable directory) the triggering operation completes normally and a warning is printed.

**Schema v1** — each line is an object with these baseline fields plus a type-specific payload:

| field  | type    | meaning                                                        |
| ------ | ------- | ------------------------------------------------------------- |
| `v`    | integer | schema version (`1`)                                          |
| `ts`   | string  | ISO-8601 UTC timestamp (seconds precision)                    |
| `repo` | string  | `project.name` from `project.yaml`, else the repo dir basename |
| `role` | string  | the identity the event belongs to (`dev` / `reviewer` / …)   |
| `type` | string  | event type (enum below)                                       |

`type` ∈ `NoteMinted`, `NoteEvolved`, `NoteSuperseded`, `NoteGraduated`, `LinkFormed`, `LinkFired`, `LinkPruned`, `RecallPerformed`, `ConsultPerformed`, `FeedbackEmitted`, `FeedbackRouted`, `FeedbackArchived`.

Payloads identify slugs / link keys / counts — **never full note bodies**. Consumers ignore unknown fields (forward-compatible). `emit_event` only builds and appends the line; wiring it into the individual commands (mint → `NoteMinted`, recall → `RecallPerformed`, …) is layered on separately.

### Recommended `/goal` for an autonomous loop

`build-next` already runs retro (step 7) and feedback (step 8) as mandatory,
in-protocol parts of every iteration — in principle nothing external should be
needed. In practice, an ambiguous skip-condition wording once let two live
repos silently never run retro despite dozens of real task closes each (fixed,
but the failure mode was invisible until specifically investigated). As
cheap, redundant insurance against that class of silent skip, set:

```
/goal /loop /build-next — after every task's iteration report, before picking
the next task, always run /spec-workflow:feedback then /spec-workflow:retrospective
(in that order — retrospective triages/mints from what feedback just emitted),
even if build-next's own retro/feedback already ran that iteration
```

Order matters — `feedback` first, `retrospective` second, since retrospective
triages and mints from whatever `feedback` just emitted (plus anything still
pending from before). `retrospective` is a safe no-op when nothing is pending
(0 pending → nothing to route or mint), so forcing it externally after every
task costs nothing when `build-next`'s own step 7 already ran cleanly, and
catches it when step 7 was silently skipped. `feedback`, forced externally
like this, WILL emit an additional record each iteration on top of step 8's
own — treat that as a deliberate second reflection pass (a meta-process check
on the loop itself), not a bug; it isn't deduped against step 8's emission.

## Human steering

- **Issue comments** — read before every task; scope changes are folded into the issue body and acknowledged.
- **Iterative UI mode** (default ON) — UI decisions go to the human via `docs/ui-options/<task-id>.html` (favorite selector + likeable aspect keywords + notes; "Copy selection" produces the comment to paste). Disable per-clone with `touch .claude/ITERATIVE_UI_OFF` or permanently with `methodology.iterativeUI: false`.
- **Checkpoint flag** — `touch .claude/CHECKPOINT` pauses the loop at the next safe boundary with a handoff.

## Local-state manifest

`scripts/local-state.manifest` is the single source of truth (SPEC-MEMORY.md §7.1/§7.4/§9.2) for every path the plugin writes at runtime, each tagged `ignore` (local state — gitignored, never committed) or `track` (shared memory — committed alongside code). Read it — never re-hardcode these paths — via `scripts/lib/local-state.sh` (bash 3.2: `spec_workflow_local_state_paths ignore|track`, `spec_workflow_local_state_policy <path>`) or `scripts/lib/local_state.py` (python stdlib: `paths()`, `policy_of()`). `scripts/gitignore-sync.sh [--dry-run] [target]` (SPEC-MEMORY.md §7.2/§7.2.1) writes the manifest's `ignore`-policy entries into a target `.gitignore` (default `.gitignore`) as a managed block between `# >>> spec-workflow managed` / `# <<< spec-workflow managed` markers — replacing only that block on a re-run (idempotent), appending it if absent, never touching a line outside the markers, and warning (path + matching rule, never editing) when a `track`-policy path is already ignored by some other non-managed rule; `setup-project`'s Phase 5 gitignore step is this script. `sync-project-configs`' `mem013-gitignore-managed-block` rule reconciles this same managed block across every already-anchored repo, keeping repos that adopted the workflow before the manifest changed in sync without a human re-running `setup-project`. The table below is kept in sync with the manifest by a test (`section-local-state-manifest.sh`) — edit the manifest, not this table.

| Policy | Path |
| --- | --- |
| ignore | `.claude/CHECKPOINT` |
| ignore | `.claude/ITERATIVE_UI_OFF` |
| ignore | `.claude/ui-hub/` |
| ignore | `.claude/gate-pass` |
| ignore | `.claude/telemetry.jsonl` |
| ignore | `.claude/lessons.jsonl` |
| ignore | `.claude/board-queue.jsonl` |
| ignore | `.claude/board-cache.json` |
| ignore | `.claude/board-comments-seen.json` |
| ignore | `.claude/neural-view/` |
| ignore | `.claude/merge-requirements.json` |
| ignore | `.claude/.flush*` |
| ignore | `.claude/worktrees/` |
| ignore | `.claude/identities/*/brain/index.sqlite3` |
| ignore | `.claude/identities/*/brain/.staleness-cache.json` |
| ignore | `.claude/assistant/` |
| track | `.claude/feedbacks/` |
| track | `.claude/identities/` |
| track | `.claude/brain-events.jsonl` |
| track | `.claude/.neural-network` |
| track | `.claude/project.yaml` |

## Telemetry

`.claude/telemetry.jsonl` (gitignored) is the loop's own performance record — SPEC §8.4. `board.sh move` and `gate.sh` append events automatically (best-effort: a write failure, e.g. a read-only `.claude`, never fails the move or the gate); `build-next` records `review-round`/`task-close` events at the orchestrator level (see its SKILL.md). Four record kinds, one JSON object per line: `transition` (`task`/`from`/`to`/`ts` — `from` is not tracked by `board.sh`, so metrics only uses `to`+`ts`), `gate` (`task`/`ok`/`ts` — `task` is the current branch name, since `gate.sh` has no task id in scope), `review-round` (`task`/`round`/`verdict`/`ts`), `task-close` (`task`/`estimate`/`ts`). `board.sh metrics` (→ `telemetry.py <root> metrics`) reports cycle time per status, first-try gate rate, rework (multi-round review) rate, and a per-task estimate-vs-actual listing; malformed lines are skipped and counted, never fatal. See `scripts/telemetry.py`'s module docstring for the exact schema.

## Neural view

The identities' **brains** (`.claude/identities/<role>/brain/`) are the workflow's memory: markdown notes wired by weighted links, plus an append-only activation log of every recall. `neural-view` is the human's window into them — a live, JARVIS-style HUD (`python3 scripts/neural-view.py start`, default `http://127.0.0.1:4748`) that draws every brain as a 3D neural cluster you navigate in space: notes are glowing neurons (size ∝ strength, graduated ones dimmed), links are synapses with a slow ambient pulse drifting source→target so the network reads as alive even at rest, and as recalls fire the page lights the neurons and sends a brighter pulse traveling the specific edge in real time — you watch a thought propagate. Top-left gauges are grouped **per repo** — one labeled section per anchored repo (`.claude/.neural-network`), each listing all three roles (orchestrator first, then alphabetical) with notes / avg strength / synapses; a role with zero notes still gets a dimmed row rather than vanishing, so a fresh repo's brains read as visibly separate from a mature one's instead of one shared trio. Notes about the same real-world entity in DIFFERENT roles' brains (`entities:` frontmatter, #163) are drawn as their own thin, low-opacity note-to-note arcs — excluded from the same-brain synapse physics, colored by default as a gradient between the two brains' colors (or a flat color via `neuralView.entityEdgeColor`). A bottom ticker streams the raw activation log, and clicking a neuron opens its note. The server is **read-only** and stdlib-only; the page makes zero external requests (the renderer is [three.js](https://threejs.org), **vendored** into `templates/vendor/` and served same-origin — see `templates/vendor/README.md` for the exact version/source/sha256 — no CDN, no build step), honors `prefers-reduced-motion`, and keeps WCAG AA text contrast. Tolerates absent brains (empty graph, "no brains yet" state) and gracefully degrades to a clear message (gauges/ticker still live) if WebGL is unavailable. Drive it with the `neural-view` skill (`start` / `stop` / `status`); `--port` and `--dir` override the defaults.

**Stale-server / zombie detection (SW-067).** `status` and `start` never report a bare STOPPED / "never bound to port" when the pidfile is missing or stale but the configured port is still occupied — they report **STALE**, naming the PID/command holding it when discoverable. The free-vs-held check is a plain stdlib bind attempt (no external tool required); the PID/command is optional enrichment via `lsof` (PID) and `ps` (full command line), degrading to "PID unknown"/"held by another process" when either is unavailable. `stop --force` stops the pidfile-tracked server if any, **and** kills a detected zombie holding the port — but only when that process's own command line contains `neural-view.py`; anything else is reported and left alone, since this is the one destructive path in an otherwise read-only tool. Plain `stop` sends SIGTERM and, since AST-010 (the `serve` branch now shuts down gracefully to join the assistant engine's worker threads instead of dying on the OS default disposition), waits — bounded, up to 5s — for the process to actually exit before reporting "stopped", so an immediate re-`start` never races a still-shutting-down process.

**3D navigation.** Drag to orbit, wheel/pinch to zoom (toward the current focus), right-drag or shift-drag to pan, click a neuron (raycast) to inspect it, and the ⌂ button or double-clicking empty space resets the camera. The view auto-fits every constellation — the whole network, with margin — on boot (even before the first `/graph` resolves), on every reload, and on resize, until you take the wheel; the fit math is a pure, hermetically-tested function of the graph's bounding sphere, not a "looked right once" heuristic. Clicking (or keyboard-focusing and pressing Enter on) a brain row in the sidebar flies the camera to frame that brain's own cluster — instantly under `prefers-reduced-motion`, eased otherwise — falling back to the repo region for a brain with zero notes; this counts as taking the wheel, same as any manual orbit/pan/zoom.

**Multi-repo aggregation.** One server can show every spec-workflow repo on the machine as "constellations" arranged on a 3D fibonacci sphere — each repo is a labeled region containing its own role-clusters, with a top-bar `repos` counter and activation-log entries tagged `[repo] ROLE · event`. Discovery: a repo is included iff `<repo>/.claude/.neural-network` marker file exists in an immediate child of the scan base (`--scan`/`$NEURAL_VIEW_SCAN`, default `~/Development`); `--dir`/`$NEURAL_VIEW_DIR` is always included regardless of the marker. A discovered repo with no brains yet still gets an empty labeled region rather than being dropped. Graph node ids are `<repo>/<role>/<slug>`; `/note/<repo>/<role>/<slug>` addresses one; `/events` cursors carry a per-repo, per-role offset. With no `--dir`/`--scan`/env at all and an empty scan base, it falls back to the git root of cwd — the original single-repo behavior.

**Hover inspection.** Moving the pointer over any element (throttled raycast) opens a small tooltip — no click needed, and it never intercepts pointer events (orbit/pan/click all work exactly as before): a note names its slug, repo/role, tags and strength; a repo region/label shows its note/brain counts plus that repo's board state (below); a synapse names its source→target, weight, and fire count. The hovered note or synapse also gets a brief visual highlight, reusing the same glow mechanism recalls use.

**Per-repo project state.** `GET /projects` reports each discovered repo's GitHub Project board — status counts, and the titles currently `In progress`/`In review` — read via **this plugin's own `board.sh`** (never `gh project` directly) invoked with `cwd=<repo root>`, so it resolves that repo's own `.claude/project.yaml`. Results are cached server-side for `$NEURAL_VIEW_PROJECTS_TTL` seconds (default 60) and the subprocess is bounded by `$NEURAL_VIEW_BOARD_TIMEOUT` seconds (default 12), so a hung `gh` call never stalls the rest of the page. A repo with no board configured is omitted; a `gh`/network failure degrades to a dim "board unavailable" badge instead of erroring. The Brains panel gets a **Boards** section (status counts + truncated task titles per repo) and the top bar gets a subtle `boards N/M` (ok/total) indicator.

**Live sessions (best effort).** `GET /sessions` surfaces locally-discoverable Claude Code sessions from `~/.claude/jobs/<id>/state.json` — background-job orchestration metadata the harness itself writes (state/cwd/a short name label/timestamps). It is **never** the conversation transcript (that lives elsewhere and is never read); only `state`/`cwd`/`name`/`createdAt` are consulted. A job counts as active if its state is `working` or it was touched within the last `$NEURAL_VIEW_SESSION_RECENT_SECS` seconds (default 900); its `cwd` is matched to a discovered repo by path prefix so the repo label picks up a `· N LIVE` suffix and the repo tooltip gets a session count. `$NEURAL_VIEW_CLAUDE_DIR` overrides `~/.claude` (mainly for tests). If no `jobs/` directory is discoverable at all, this simply returns `[]`.

## Testing

```bash
bash tests/run-tests.sh        # hermetic: validator fixtures, picker, preflight, init-config (fake gh)
python3 tests/check-evals.py   # eval case structure
claude plugin eval .           # skill evals on real models (early access)
```

`run-tests.sh` is a thin runner: it defines the shared `check`/`check_rc`/`check_absent`/
`lifecycle_start` helpers (in `tests/_lib.sh`) and sources every `tests/section-*.sh` file
in a fixed order. Each section file owns one area (config, gate enforcement, neural-view,
brain, etc.) and is sourced, not run standalone — it assumes `_lib.sh` and the runner's
shared state (`HERE`/`PLUGIN`/`FIX`/`fails`/`flaky`) are already in scope. To add a test to
an existing area, add a `check ...` line to that area's `section-*.sh`; to add a new area,
create a new `section-<area>.sh` (with the standard sourced-not-standalone header comment)
and register it in `run-tests.sh`'s `SECTIONS` array. This split keeps parallel build-loop
lanes from all appending to one file (a guaranteed rebase conflict on every merge).

Every `section-*.sh` also carries a one-line standalone-run guard as its first executable
statement (`declare -F check … || { echo "section files are sourced by run-tests.sh; run: …"; exit 2; }`):
run a section file directly and it prints a pointer to the runner and exits 2, instead of a
wall of `check: command not found` / empty-`$PLUGIN` path noise (#94). `section-section-guard.sh`
is the meta-test that pins this — a glob asserting every section file carries the guard, plus a
behavioral check that running one directly actually exits 2 with the pointer. When you add a new
section file, include the guard line (copy it from any existing section) or that meta-test fails.

Fake-`gh` fixtures that simulate a `gh` failure (masked rate limits, honest rate limits,
etc.) source their trigger text from `tests/fixtures/gh-failures/` — a corpus of REAL
captured `gh` outputs with provenance — rather than inlining invented strings beside the
classifier they test (see that directory's `README.md`). Whenever a masked or odd `gh`
error is observed live in any session, capture the raw bytes there immediately.

To verify a single-area change without paying the whole suite's minutes, filter with
`--section`: `bash tests/run-tests.sh --section board-queue` runs only sections whose
base-name (file name minus the `section-` prefix and `.sh` suffix) contains the argument as
a **substring** — so `--section board` runs every `board-*` section. The flag is repeatable
and comma-separated (`--section a,b`, `--section a --section b`), the union runs in the
registered order (preserving the ui-hub → neural-view-lifecycle pairing when both are
selected), and an argument matching nothing errors with the list of available sections. The
`SPEC_TESTS_SECTION` env var is an equivalent filter source. **Filtering is for local
iteration only**: the recorded gate pass must always be a full-suite run, so `gate.sh`
refuses to run at all when `SPEC_TESTS_SECTION` is set.
